test_arch/
│
├── configs/                   # 配置参数
│   ├── rules.json                  # 规则表，定义建筑类型与计算器类的映射关系（动态调度）
│   ├── base_config.toml                 # 定义系统级参数，定义单位、比例、默认参数
│   ├── geometry_config.toml    # 提供构件尺寸比例信息，如大式、小式的檐柱信息
│   ├── house_config.toml           # 定义具体建筑形式属性（形制参数），如斗拱、飞檐、模数系统等
│   ├── config_manager.py           # 通用配置加载器，加载base_config.toml、house_config.toml、style_config.toml等
│   └── rule_manager.py          # 规则调度，从rules.json读取建筑类型→计算器类映射关系
│
├── data/                        # 数据存储（CSV/XLS）
│   └── data.csv                # 园林建筑基本数据，每一行代表一个建筑
│ 
├── core/
│    ├── __init__.py
│    ├── data_loader.py                 #数据加载器，初步结构化数据，返回basic_info、structure_info、description_info、dimension_info、structure_name
│    ├── config_loader.py               #配置加载器，输入structure_name，查找configs对应的配置信息（预设信息、建筑信息）
│    ├── calculator_factory.py           #决定“实例化谁”；
│    ├── component_calculator.py        #决定“怎么算”；数据计算，输入dimension_info，输出eave_diameter、eave_height、num_purlins、ridge_distance、x_grid、y_grid、pillars_coords、beams_coords、主要标高信息等建模需要的关键信息
│    └── calculators/
│         ├── __init__.py
│         ├── base_calculator.py
│         ├── house_calculator.py
│         └── pavilion_calculator.py
│
├── structure/
│   ├── utils.py                # 含mesh_pool和collection_init功能，输入basic_info、structure_info，初始化集合，以及检查创建的mesh对象，如果bpy.data.meshes中有则返回，没有则创建
│   ├── assembler.py           # 输入主要标高信息和构件网格对象，组合功能创建组合，输入description_info（含 placement 功能 ）判断正房、厢房、倒座房
│   ├── frame/                #改名为 frame,与子模块统一名称
│   │   ├── pillar_frame.py       # 改名为pillar_frame，统一名称，输入pillars_coords、components对象，遍历位置创建柱网
│   │   ├── beam_frame.py
│   │   └── roof_system.py
│   ├── components/             #创建bmesh对象，返回mesh\object对象，不链接到集合
│   │   ├── pillar.py          # 输入eave_diameter、eave_height，返回檐柱对象，应考虑柱类，根据檐柱高柱径返回金柱、瓜柱、童柱等对象
│   │   ├── beam.py
│   │   ├── roof.py
│   │   └── __init__.py
│   └── __init__.py
│
├── tests/ 
│   └── test
│
├── generator.py               # 主生成器
├── main.py                    # 程序入口
│
│   ..........................................................
│
├── ai/                              # AI 扩展模块
│   ├── __init__.py
│   ├── param_fitter.py              # 参数拟合（根据数据预测斗口、坡度等）
│   ├── feature_extractor.py         # 特征提取（结构 + 装饰特征）
│   ├── layout_optimizer.py          # 平面与立面布局优化
│   └── style_transfer.py            # 风格迁移（唐/宋/清风格自动调整）
│
├── io/                              # 输入输出层（与外部系统对接）
│   ├── __init__.py
│   ├── ifc_exporter.py              # IFC/BIM 文件导出
│   ├── cad_exporter.py              # DWG/DXF 工程图导出
│   ├── svg_generator.py             # 草图生成（参数线稿）
│   ├── json_io.py                   # JSON、YAML 统一加载器
│   └── dataset_exporter.py          # 导出AI训练数据集
│
├── visualization/                   # 交互与展示层
│   ├── ui_panel.py
│   ├── component_panel.py
│   ├── preset_manager.py
│   ├── preview_renderer.py
│   └── engineering_drawer.py        # 生成工程图（线稿 + 尺寸）


这是一个使用blender api的传统木结构建筑生成项目，以上是项目设计架构，
项目中涉及大量的object\mesh\collection以及园林名称、建筑名称、建筑类型等，尤其是mesh会有大量复用，给我一个规范化的命名规则


                 ┌────────────────────────┐
                 │         main.py         │
                 │  程序入口：加载CSV、调用generator │
                 └──────────┬─────────────┘
                            │
                            ▼
               ┌────────────────────────┐
               │      generator.py       │
               │  调度主模块：组织建模流程 │
               │  调用configs、core、structure等 │
               └──────────┬─────────────┘
                            │
     ┌──────────────────────┼────────────────────────────┐
     │                      │                            │
     ▼                      ▼                            ▼
┌──────────────┐     ┌────────────────┐          ┌─────────────────┐
│ configs/      │     │ core/          │          │ structure/       │
│ 配置与规则系统 │     │ 计算逻辑层       │          │ 结构生成与定位层   │
└─────┬──────────┘     └────┬───────────┘          └────┬────────────┘
      │                      │                           │
      │                      │                           │
      ▼                      ▼                           ▼
┌──────────────┐     ┌────────────────┐          ┌──────────────────┐
│ config_manager│     │ calculator_factory│       │ collection.py     │
│ 读取toml/json配置 │  │ 根据rules分发计算器类 │   │ 建立对象集合         │
└──────────────┘     └────────────────┘          └──────────────────┘
│
├── rule_manager.py    ← 从 rules.json 中读取建筑类型 → 计算器映射
│
├── base_config.toml   ← 定义系统默认参数（单位、比例）
│
├── house_config.toml  ← 定义各类型建筑样式（has_dougong, modular_system等）
│
└── style_config.toml  ← 定义doukou_style, large_style, small_style等风格特征